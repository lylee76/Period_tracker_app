<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Last Period -->
    <TextView
        android:id="@+id/tvLastPeriod"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Last Period:"
        android:textSize="18sp" />

    <!-- Next Period Prediction -->
    <TextView
        android:id="@+id/tvNextPeriod"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Next Period:"
        android:textSize="18sp"
        android:layout_marginTop="8dp"/>

    <!-- Cycle Status (Regular/Irregular) -->
    <TextView
        android:id="@+id/tvCycleStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cycle Status:"
        android:textSize="16sp"
        android:layout_marginTop="8dp"/>

    <!-- Add Period Button -->
    <Button
        android:id="@+id/btnAddPeriod"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Period Date"
        android:layout_marginTop="20dp"/>

    <!-- Calendar Button -->
    <Button
        android:id="@+id/btnCalendar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Calendar"
        android:layout_marginTop="10dp"/>

    <!-- Articles / Awareness Button -->
    <Button
        android:id="@+id/btnArticles"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Health Articles"
        android:layout_marginTop="10dp"/>
</LinearLayout>
